using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 艾迪亚UnityFluidPro : MonoBehaviour
{
    public enum Direction { 中点法, 自适应改变步长, 隐式实现方法 };
    public Direction 选择实现曲线方法;
    public int 方向;
    public int 角度;
    public int 段数;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
